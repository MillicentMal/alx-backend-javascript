import ClassRoom from "./0-classroom.js";

export default function initializeRooms() {
 
  x =  new ClassRoom(19),
  y= new Classroom(20),
   z = new Classroom(34);
  classes = [x, y, z];
  return classes;
}
  
